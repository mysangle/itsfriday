import { SunDimIcon } from "lucide-react";

const Empty = () => {
  return (
    <div className="mx-auto">
      <SunDimIcon strokeWidth={0.5} absoluteStrokeWidth={true} className="w-24 h-auto text-muted-foreground" />
    </div>
  );
};

export default Empty;
